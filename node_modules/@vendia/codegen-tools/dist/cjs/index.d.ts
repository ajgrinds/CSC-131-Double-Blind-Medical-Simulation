import { Options as Options$1 } from 'tsup';

declare const compileTs: (entry: string, version: string, options: Options$1) => Promise<void>;

interface Options {
    dotVendiaDirPath: string;
    debugInfo?: any;
    debug?: any;
    vendiaMonorepoOverrides?: {
        dotVendiaClientPath: string;
        installedClientPath: string;
    };
    version: string;
}
declare const generateClient: ({ dotVendiaDirPath, debugInfo, debug, vendiaMonorepoOverrides, version, }: Options) => Promise<void>;

declare const generateTypescript: ({ graphQlSchema, schemaJson, schemaNamespace }: any) => Promise<string>;

export { compileTs, generateClient, generateTypescript };
